android.buildTypes {

    def BOOLEAN = "boolean"
    def FEATURE_BEACONS = "FEATURE_BEACONS"
    def TRUE = "true"
    def FALSE = "false"

    debug {
        buildConfigField BOOLEAN, FEATURE_BEACONS, TRUE
    }

    release {
        buildConfigField BOOLEAN, FEATURE_BEACONS, FALSE
    }
}